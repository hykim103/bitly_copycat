<nav class="flex justify-between items-center py-5 px-10 bg-orange-100">
  <%= link_to user_signed_in? ? url_path(Url.first) : root_path do %>
    <div class="flex items-center">
      <%= image_tag "logo", class: "w-8" %>
      <p class="ml-3 text-lg title">copycat</p>
    </div>
  <% end %>

  <div>
    <% if user_signed_in? %>
      <div class="flex items-center">
        <p class="text-xl mr-3">Hi, <span class="font-bold"><%= current_user.username %></span>!</p>
        <%= link_to "Sign out", destroy_user_session_path, data: {turbo_method: :delete}, class: "btn px-5 py-3" %>
      </div>
    <% else %>
      <%= link_to "Sign in", new_user_session_path, class: "bg-orange-500 px-5 py-3 rounded-lg text-white" %>
    <% end %>
  </div>
</nav>
